name: Implementador
# DISABLED: requiere opencode CLI (API externa)
# Reemplazar con agente local cuando esté disponible
on:
  workflow_dispatch:

jobs:
  implement:
    runs-on: ubuntu-latest
    steps:
      - name: DISABLED — 100% local Ollama
        run: |
          echo "⚠️ Este workflow requiere opencode CLI (API externa)."
          echo "Deshabilitado para cumplir con politica 100% local Ollama."
          echo "Los issues se implementan via agente local en VPS."
