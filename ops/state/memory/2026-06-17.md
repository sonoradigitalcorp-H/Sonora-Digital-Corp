# Session Memory — 2026-06-17

## Swarm Rebirth — Enterprise OS

**Context**: User requested fix & synchronization of OpenClaw, Hermes, and opencode under the new Enterprise OS truth after `sonora-enterprise-os/` migration.

### Key Actions

1. **Created `TRUTH.md`** (`sonora-enterprise-os/constitution/TRUTH.md`) — canonical source for all agents: path mappings, ports, services, constitution summary

2. **Fixed `opencode.json`**:
   - Added `TRUTH.md` as first instruction (highest priority)
   - Fixed `external_directory` deny path from old `/home/mystic/sdcorp/.secrets` → `/home/mystic/sonora-digital-corp/config/.secrets`
   - Updated service descriptions to match reality

3. **Fixed OpenClaw skills** (`/home/mystic/.openclaw/workspace/skills/`):
   - `auto-save/SKILL.md`: All `/home/mystic/jarvis/` paths → `/home/mystic/sonora-digital-corp/`
   - `ceo-workflow/SKILL.md`: Fixed DOCUMENTO_DE_ERRORES path, deploy paths, products paths

4. **Fixed Hermes skills** (`/home/mystic/.hermes/skills/`):
   - `sdc-cron/SKILL.md`: 30+ old `~/jarvis/` → full paths, `cd ~/jarvis && docker compose` → `docker compose -f infra/docker-compose.yml`

5. **Fixed Hermes cron** (`/home/mystic/.hermes/cron/jobs.json`):
   - Updated `~/jarvis/hermes_bridge.py` → `apps/hermes/hermes_bridge.py` in health monitor prompt

6. **Fixed Web UI** (`apps/webui/fastapp.py`):
   - Added `apps/jarvis/` to `sys.path` — fixed `ModuleNotFoundError: No module named 'src'`
   - Web UI now starts successfully on port 5174

7. **Fixed daily pipeline** (`scripts/daily-pipeline.sh`):
   - Healthcheck URL: `/health` → `/api/status` (correct endpoint)

### Audit Results

| Service | Port | Status |
|---------|------|--------|
| OpenClaw | 18789 | ✅ UP (55 skills in gateway, 42 on disk) |
| Hermes | 8000 | ✅ UP (v3.3.6, 21 skills) |
| Web UI | 5174 | ✅ UP (v2.0.0) |
| Qdrant | 6333 | ✅ UP (v1.7.4) |
| Neo4j | 7687 | ✅ UP (healthy) |
| n8n | 5678 | ⏸️ Not started |

**Pipeline**: 6/6 checks pass, 42s runtime

### Paths Established (NO EXCEPTIONS)
- Old `/home/mystic/jarvis/` → `/home/mystic/sonora-digital-corp/`
- Old `/home/mystic/sdcorp/` → `/home/mystic/sonora-digital-corp/`
- Docker compose: `infra/docker-compose.yml`
- Hermes bridge: `apps/hermes/hermes_bridge.py`
- Backups: `state/backups/`

### Key Commands
```bash
cd /home/mystic/sonora-digital-corp
bash scripts/daily-pipeline.sh
pytest tests/unit/ -q
```
