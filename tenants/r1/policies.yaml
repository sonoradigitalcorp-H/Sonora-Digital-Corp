prompt_injection:
  enabled: true
  model: mystic-shield

rate_limits:
  whatsapp: 120/minute
  api_calls: 500/hour

data_masking:
  pii_detection: true
  auto_redact:
    - credit_card
    - phone

tool_sandbox:
  enabled: true
  allowed_domains:
    - "api.ceson.mcp"
    - "mercadopago.com"
  blocked_commands:
    - rm
    - sudo
